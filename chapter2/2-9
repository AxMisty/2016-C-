#include <iostream>  
#include <string>  

using namespace std;  

int main(){
	int a,b;
	cin>>a>>b;
	if(a<b)
	cout<<b<<"较大";
	else{ 
		if(a>b)
		cout<<a<<"较大";
		else
		cout<<a<<"与"<<b<<"一样大"; } 
	return 0;
}
